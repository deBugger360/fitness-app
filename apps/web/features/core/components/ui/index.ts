/**
 * apps/web shared UI primitives
 *
 * These match the React Native equivalents exported from @repo/ui/components,
 * but are implemented using Framer Motion + Tailwind CSS for the web.
 *
 * Design contract is shared — same visual output, platform-appropriate rendering.
 *
 * Import from '@/features/core/components/ui' in web components.
 */

export { Card } from './Card';
export { Button } from './Button';
export { ProgressRing } from './ProgressRing';
export { SectionHeader } from './SectionHeader';
export { EmptyState } from './EmptyState';
